matrix:
  platform: [macos_arm64, macos]
  bazel: [7.x, 8.x]
tasks:
  verify_targets:
    name: Build and test
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@rules_swift_previews//..."
    test_targets:
      - "@rules_swift_previews//tests/..."
